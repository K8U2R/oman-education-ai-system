# 📝 الدليل المرجعي للأوامر السيادية (Sovereign Commands Reference)

> **⚠️ تنبيه هام:** هذا هو المصدر الوحيد للحقيقة لتشغيل النظام. يُمنع استخدام أي أوامر غير مذكورة هنا.

## 1. مبادئ التشغيل (Law 11)

وفقاً للمادة 11 من الدستور (Root Sovereignty):

* ✅ **يجب** تشغيل كافة الأوامر من المجلد الجذري للمشروع.
* ✅ **يجب** استخدام `--prefix` أو `npm run <script>` المعرفة مسبقاً.
* ⛔ **يُحظر** تماماً استخدام `cd` للدخول للمجلدات الفرعية يدوياً.
* ⛔ **يُحظر** تشغيل `npm start` أو `npm dev` من داخل `frontend/` أو `backend/` مباشرة.

---

## 2. جدول الأوامر المعتمدة

| الأمر | الوصف | النطاق |
| :--- | :--- | :--- |
| `npm run dev` | **التشغيل الكامل:** يطلق قاعدة البيانات، الباك إند، الفرونت إند، وقمرة المطور في آن واحد (متزامن). | النظام كامل |
| `npm run dev:cockpit` | **قمرة المطور:** تشغيل لوحة المطورين فقط (Port 3002) بشكل معزول. | أدوات |
| `npm run db:init` | **تهيئة القاعدة:** إنشاء الجداول وتشغيل الـ Seeders الأساسية. | قاعدة البيانات |
| `npm run db:reset` | **تصفير شامل:** حذف قاعدة البيانات وإعادة بنائها من الصفر (خطر!). | قاعدة البيانات |
| `npm run validate` | **الفحص الشامل:** تشغيل اختبارات Type Check, Lint, Unit Tests لكل النظام. | CI/CD |
| `npm run build` | **بناء النظام:** بناء نسخ الإنتاج (Production Build) للفرونت والباك. | نشر |

---

## 3. أمثلة عملية (Correct Usage)

### ✅ الطريقة الصحيحة (Sovereign Way)

```bash
# تشغيل بيئة التطوير
npm run dev

# فحص ملفات الفرونت إند فقط
npm run type-check:frontend

# إصلاح مشاكل التنسيق في الباك إند
npm run lint:backend -- --fix
```

### ❌ الطريقة المحظورة (Legacy Way)

```bash
# لا تفعل هذا أبداً!
cd frontend
npm run dev

# هذا يكسر تزامن العمليات ويخالف الدستور!
```

> **تم التحديث:** 2026/01/26 - متوافق مع الدستور v5.0
