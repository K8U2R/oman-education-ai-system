# 02_DEPARTMENT_PROTOCOLS.md - بروتوكولات الأقسام 👮‍♂️

> **الهدف:** تحديد القواعد الصارمة التي يجب على كل فريق الالتزام بها لضمان الجودة والتناغم.

---

## 🎨 قسم الواجهة الأمامية (Frontend Department)

### 1. الاتساق البصري أولاً (Visual Consistency First)

يمنع منعاً باتاً اختراع ألوان أو قياسات جديدة. يجب الالتزام الصارم بملف `STYLES_AND_TOKENS.md`.

* ❌ `style={{ color: '#334455' }}`
* ✅ `className="text-primary-700"`

### 2. الوعي بالصلاحيات (Permission Awareness)

لا يجوز عرض زر أو رابط لمستخدم لا يملك صلاحية استخدامه.

* **القاعدة:** جميع العناصر الحساسة يجب أن تكون مغلفة بـ `<PermissionGuard required={['permission.name']} />`.
* المكون لا يخفي نفسه بـ `if` بسيطة، بل يستخدم نظام الصلاحيات المركزي.

---

## ⚙️ قسم الخلفية (Backend Department)

### 1. الدفاع الصامت (Silent Defense)

لا يجوز أبداً تسريب تفاصيل الخطأ التقني (Stack Trace) للمستخدم النهائي.

* ❌ `res.status(500).send(err.message)`
* ✅ `next(new AppError('Something went wrong', 500))` (والميدل وير يتولى التنظيف).

### 2. القياس عن بعد (Telemetry)

كل عملية حساسة (تعديل بيانات، دخول، دفع) يجب أن تولد "حدث سجل" (Log Event) مهيكل.

* يجب أن يحتوي السجل على: `actor_id`, `action`, `resource`, `status`.

---

## 🤖 قسم الذكاء الاصطناعي (AI Department)

### 1. الحياد التقني (Vendor Agnostic)

يمنع استيراد مكتبات محددة (مثل `openai` sdk) داخل خدمات التطبيق (Use Cases).

* **القاعدة:** يجب استخدام نمط المحول (Adapter Pattern).
* الخدمة تطلب `AIService.generate()`، والمحول في `Infrastructure` يقرر هل يستخدم GPT-4 أم DeepSeek.

### 2. الفشل الآمن (Graceful Degradation)

إذا فشل نموذج AI في الرد، يجب أن يكون هناك خطة بديلة (Fallback) أو رسالة خطأ لطيفة، ولا ينهار النظام بالكامل.
