# 🏛️ الدستور السيادي الموحد (Unified Sovereign Constitution v3.0)

**المشروع:** نظام عمان التعليمي للذكاء الاصطناعي (Oman AI Education System)
**الحالة:** نافذ فوراً (Immediately Effective)
**مستوى الصرامة:** مطلق (Absolute)

> **المقدمة:**
> هذا الدستور ليس مجرد إرشادات، بل هو "إطار تشريعي صارم" يضمن بقاء النظام متماسكاً، آمناً، وقابلاً لاستيعاب ملايين الطلاب دون ديون تقنية. أي كود ينتهك مادة من هذه المواد يُعتبر "فاسداً" ويجب رفضه في مرحلة المراجعة (Code Review).

---

## 🏗️ الباب الأول: السيادة المعمارية (Architectural Sovereignty)

### ⚖️ مادة (1): قانون العزل الصارم (The Iron Firewall)

* **النص:** يحظر تماماً أي استيراد مباشر (Import) للكود أو الأنواع (Types) بين `backend` و `frontend`، أو بين النواة `core` والإضافات الخارجية. يجب أن تظل الطبقات معزولة فيزيائياً ومنطقياً.
* **السبب (Rationale):** لضمان أن تغيير قاعدة البيانات أو منطق الخادم لا يؤدي إلى انهيار واجهة العميل، ولتمكين النشر المستقل (Independent Deployment) لكل جزء.
* **طريقة التنفيذ (Enforcement):**
    1. تفعيل قاعدة ESLint: `@nrwl/nx/enforce-module-boundaries`.
    2. أي Pull Request يحتوي على استيراد عابر للحدود (Cross-boundary import) يتم رفضه تلقائياً.

### ⚖️ مادة (2): قانون الاستيراد المباشر (Direct Import Law)

* **النص:** داخل النطاق الواحد (Module)، يُمنع استخدام ملفات `index.ts` (Barrel Files) لتجميع التصديرات الداخلية. الاستيراد يجب أن يكون من الملف المصدري مباشرة، بينما تُستخدم الـ Barrels فقط للتصدير **لخارج** النطاق.
* **السبب (Rationale):** الاستخدام المفرط للـ Barrels داخلياً يسبب مشاكل التبعية الدائرية (Circular Dependencies) ويعيق عملية الـ Tree-Shaking مما يبطئ الأداء.
* **طريقة التنفيذ (Enforcement):** استخدام أدوات التحليل السكوني (Static Analysis) لمنع استيراد المسارات التي تنتهي بمجلد بدلاً من ملف داخل نفس النطاق.

---

## 🌐 الباب الثاني: حوكمة الخدمات (Service Governance)

### ⚖️ مادة (3): قانون سيادة العناقيد (Cluster Sovereignty)

* **النص:** يُحظر الاتصال المباشر بين الخدمات (Services) التي تنتمي لعناقيد مختلفة (مثلاً: خدمة `education` تستدعي `auth repository` مباشرة). الاتصال مسموح فقط عبر "واجهات الخدمة العامة" (Public Service Interfaces).
* **السبب (Rationale):** الهيكل الحالي يعتمد على عناقيد مستقلة (`auth`, `user`, `ai`). التداخل المباشر يخلق "جحيم الترابط" (Coupling Hell) ويمنع تحويل النظام إلى Microservices مستقبلاً.
* **طريقة التنفيذ (Enforcement):**
    1. تغليف كل عنقود بـ `ServiceFacade` (واجهة موحدة).
    2. رفض أي كود يستدعي `Repository` لا يتبع لنفس الـ Domain.

### ⚖️ مادة (4): قانون عقود البيانات (Data Contract Law)

* **النص:** جميع البيانات التي تخرج عبر الـ API يجب أن تكون مغلفة في كائنات نقل البيانات (DTOs) المعرفة في `api/dto`. يُمنع منعاً باتاً تمرير كائنات قاعدة البيانات الخام (Raw Entities) إلى الواجهة الأمامية.
* **السبب (Rationale):** لمنع تسريب بيانات حساسة (مثل Hashed Passwords) ولضمان ثبات العقد (Contract Stability) مع الـ Frontend حتى لو تغير شكل قاعدة البيانات.
* **طريقة التنفيذ (Enforcement):** استخدام مكتبات `class-transformer` لتحويل البيانات قبل إخراجها من الـ Controller.

---

## 🤖 الباب الثالث: تشريعات الذكاء الاصطناعي (AI Legislation)

### ⚖️ مادة (5): قانون مكافحة الهلوسة (Anti-Hallucination Protocol)

* **النص:** يُحظر تخزين أو عرض أي مخرج من `ai-kernel` للمستخدم النهائي دون المرور عبر طبقة "التحقق الهيكلي" (Structural Validation Layer).
* **السبب (Rationale):** النماذج اللغوية (LLMs) غير حتمية وقد تولد JSON غير صالح أو محتوى ضار. يجب ضمان أن المخرجات تلتزم بالمخطط (Schema) المتوقع بدقة.
* **طريقة التنفيذ (Enforcement):**
    1. استخدام مكتبة `Zod` لتعريف المخططات المتوقعة للمخرجات.
    2. تطبيق منطق إعادة المحاولة (Retry Logic) تلقائياً عند فشل التحقق.

### ⚖️ مادة (6): قانون العزل عن البيانات الخام (Raw Data Isolation)

* **النص:** يُمنع لوكلاء الذكاء الاصطناعي (AI Agents) الوصول المباشر للكتابة في قاعدة البيانات الأساسية. القراءة تتم عبر محولات مخصصة للقراءة فقط (ReadOnly Adapters).
* **السبب (Rationale):** لمنع الذكاء الاصطناعي من تعديل السجلات بشكل غير مقصود أو الوصول لبيانات شخصية (PII) غير مصرح بها أثناء الاستنتاج.
* **طريقة التنفيذ (Enforcement):** حقن `IReadOnlyRepository` للـ Agents التي لا تحتوي على دوال `create` أو `delete`.

---

## 🛡️ الباب الرابع: قضاء الجودة والاختبار (Quality Jurisdiction)

### ⚖️ مادة (7): هرم الاختبار الإلزامي (The Testing Pyramid)

* **النص:** تُصنف المجلدات حسب الأهمية، ويُفرض نوع الاختبار بناءً على التصنيف:
  * **المناطق الحرجة (Critical):** (`auth`, `payment`, `ai-kernel`) -> تتطلب 100% تغطية (Unit + Integration).
  * **المناطق التشغيلية (Operational):** (`handlers`, `services`) -> تتطلب اختبارات تكامل (Integration Tests).
* **السبب (Rationale):** الموارد محدودة؛ يجب تركيز الجهد الهندسي على المناطق التي تسبب كارثة في حال فشلها.
* **طريقة التنفيذ (Enforcement):** إعداد `Jest` لرفض الـ Commit إذا قلت التغطية في المجلدات الحرجة عن النسبة المحددة.

### ⚖️ مادة (8): قانون المحاكاة (Mocking Law)

* **النص:** يمنع استخدام خدمات الذكاء الاصطناعي الحقيقية (OpenAI/Anthropic APIs) أثناء تشغيل اختبارات الوحدة (Unit Tests). يجب استخدام `Mock Agents`.
* **السبب (Rationale):** لتجنب التكاليف الباهظة، البطء، وعدم الحتمية (Non-determinism) أثناء التطوير اليومي.
* **طريقة التنفيذ (Enforcement):** استخدام `FakeAIProvider` في بيئة الاختبار (Testing Environment).

---

## 📝 الباب الخامس: التوثيق والشفافية (Documentation Mandates)

### ⚖️ مادة (9): قانون التوثيق المتزامن (Synchronized Documentation)

* **النص:** أي تعديل في ملفات `api/dto` أو `handlers` يجب أن يرافقه تعديل فوري وتلقائي في ملفات `swagger` التعريفية.
* **السبب (Rationale):** التوثيق القديم يضلل المطورين ويسبب أخطاء تكامل. يجب أن يكون التوثيق مرآة حية للكود.
* **طريقة التنفيذ (Enforcement):** الاعتماد على التوليد التلقائي (Code-First Approach) أو فشل الـ CI Pipeline إذا اختلف الـ Spec عن الكود.

### ⚖️ مادة (10): قانون التعليق الهادف (Purposeful Commenting)

* **النص:** يحظر استخدام التعليقات التي تشرح "ماذا" يفعل الكود. التعليقات يجب أن تشرح "لماذا" تم اتخاذ هذا القرار (Design Decision) أو شرح الخوارزميات المعقدة فقط.
* **السبب (Rationale):** الكود النظيف يشرح نفسه. التعليقات الزائدة تشوش القراءة وتصبح قديمة بسرعة.
* **طريقة التنفيذ (Enforcement):** المراجعة البشرية (Code Review).

---

## ⚙️ الباب السادس: العمليات التشغيلية (Operational Protocols)

### ⚖️ مادة (11): قانون التشغيل المركزي (Root Sovereignty)

* **النص:** تُنفذ جميع أوامر التشغيل، الاختبار، والترحيل (Migration) حصرياً من الجذر (Root Directory) باستخدام سكربتات `package.json` الموحدة.
* **السبب (Rationale):** لضمان تحميل متغيرات البيئة (`.env`) بشكل صحيح وتوحيد بيئة العمل لجميع المطورين.
* **طريقة التنفيذ (Enforcement):** توثيق جميع الأوامر في ملف `README.md` الرئيسي ومنع السكربتات الفرعية العشوائية.

### ⚖️ مادة (12): قانون عدم التكرار المطور (Refined DRY Law)

* **النص:** قبل كتابة أي دالة مساعدة (Utility) جديدة، يجب البحث في مجلد `shared/utils`. إعادة اختراع العجلة تعتبر فشلاً معمارياً.
* **السبب (Rationale):** لتقليل الديون التقنية، تسهيل الصيانة، وتوحيد السلوك في النظام.
* **طريقة التنفيذ (Enforcement):** الفحص الدوري للكود المتشابه (Code Duplication Detection).

---
*تمت المصادقة على هذا الدستور بتاريخ 2026-01-23.*
*التوقيع: مكتب الهندسة المعمارية (Architectural Office)*
