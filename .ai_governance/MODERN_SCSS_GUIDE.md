# دليل SCSS الحديث (Modern Sass Guide - 2026 Edition) 📘✨

> **الهدف:** توثيق المعايير الحديثة (Dart Sass) لضمان الأداء، قابلية الصيانة، وتجنب الدوال المهجورة (Deprecated).

---

## 1. وداعاً `@import` .. أهلاً `@use` 👋

### القاعدة

يُمنع استخدام `@import` تماماً. يجب استخدام `@use` لدمج الملفات.

### لماذا؟

* **عزل النطاق (Namespacing):** يمنع تلوث النطاق العام (Global Scope).
* **الأداء:** يمنع تحميل نفس الملف عدة مرات.

### المثال العملي

❌ **قديم:**

```scss
@import 'abstracts/tokens';
.btn { color: $color-primary; }
```

✅ **حديث:**

```scss
@use 'abstracts/tokens'; 
.btn { color: tokens.$color-primary; }
```

---

## 2. الدوال المدمجة (The Built-in Modules Rule) 🛠️

النسخ الحديثة من Sass ألغت الدوال العامة (Global Functions) مثل `lighten`, `darken`, `map-get`. يجب استيراد الوحدات الخاصة بها.

### كيفية الاستخدام

يجب استدعاء الوحدة أولاً: `@use 'sass:color';` أو `@use 'sass:map';`.

| الدالة القديمة (المحظورة ❌) | الدالة الحديثة (المعتمدة ✅) | الملاحظات |
| :--- | :--- | :--- |
| `lighten($color, 10%)` | `color.scale($color, $lightness: 10%)` | `lighten` تعمل بطريقة خطية غير دقيقة. `scale` أدق بصرياً. |
| `darken($color, 10%)` | `color.scale($color, $lightness: -10%)` | -- |
| `map-get($map, $key)` | `map.get($map, $key)` | يجب استيراد `@use 'sass:map';` |

### مثال توضيحي

```scss
@use 'sass:color';
@use 'abstracts/tokens' as *;

.btn-hover {
  // ❌ خطأ: lighten غير موجودة
  // background: lighten($primary, 10%);
  
  // ✅ صح: استخدام الوحدة
  background: color.scale($primary, $lightness: 10%);
}
```

---

## 3. تحذيرات الأداء (Performance Hazards) ⚡

### أ. حظر `@extend` 🚫

تجنب استخدام `@extend` تماماً في المشاريع الكبيرة.

* **السبب:** تقوم بإنشاء سلاسل CSS طويلة ومعقدة (`.a, .b, .c, .d { ... }`) يصعب تتبعها وتزيد حجم الملف بشكل غير متوقع.
* **البديل:** استخدم `@mixin`. الكود المكرر في Sass (Gzip) أفضل من التشابك في CSS.

### ب. Sass vs Tailwind vs CSS Native 🥊

لا تستخدم Sass للعمليات الحسابية المعقدة التي يمكن للمتصفح القيام بها.

* **متغيرات Sass (`$var`):** تُحسب أثناء البناء (Compile Time). استخدمها *للتنظيم فقط* (Breakpoints, Grid configurations).
* **متغيرات CSS (`--var`):** تُحسب أثناء التشغيل (Runtime). **هي الأساس** للألوان والثيمات لأنها تتغير فوراً دون إعادة بناء.
* **الحسابات:**
  * ❌ `width: $width / 2;` (Sass) - القيمة تثبت ولا تتغير.
  * ✅ `width: calc(var(--width) / 2);` (CSS) - ديناميكية ومتجاوبة.

---

## 4. ترتيب الاستيراد (The Golden Order) ⚠️

لتجنب خطأ `@use rules must be written before any other rules`:

1. وحدات Sass المدمجة (`@use 'sass:math'`).
2. ملفات المشروع ومكتباته (`@use 'abstracts/mixins'`).
3. توجيهات `@tailwind`.
4. كود CSS العادي.

---

## الخلاصة للمطور 👷‍♂️

1. استخدم `map.get` وليس `map-get`.
2. استخدم `color.scale` وليس `lighten`.
3. لا تستخدم `@extend`.
4. اجعل `@use` دائماً في السطر رقم 1.

> **آخر تحديث:** 2026/01/26 - متوافق مع Dart Sass 1.70+
