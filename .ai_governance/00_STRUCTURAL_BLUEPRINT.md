# 00_STRUCTURAL_BLUEPRINT.md - المخطط الهيكلي الثابت 🏗️

> **الحالة:** وثيقة دستورية (Immutable)
> **تاريخ الاعتماد:** 2026-01-26
> **الهدف:** توحيد الهيكلية ومنع الفوضى المعمارية.

---

## 🏛️ الفلسفة الجذرية (Root Philosophy)

يعتمد المشروع على بنية **Monorepo** مقسمة بصرامة (Strict Separation Concerns). يمنع تداخل المسؤوليات بين الطبقات.

### المجلدات السيادية

1. **`backend`**: العقل المدبر والنواة (Node.js/Express + Clean Architecture).
2. **`frontend`**: واجهة التفاعل (React/Vite + Atomic Design).
3. **`database-core`**: البنية التحتية للبيانات (PostgreSQL + Redis).

---

## ⚙️ تشريح الخلفية (Backend Anatomy)

نتبع **Clean Architecture/Hexagonal Architecture** بحذافيرها.

### 1. `src/application` (القلب النابض) ❤️

* يحتوي على **حالات الاستخدام (Use Cases)** فقط.
* يمثل "المنطق التجاري" (Business Logic).
* **قاعدة:** ممنوع استيراد أي شيء من `presentation` أو `infrastructure` هنا.

### 2. `src/presentation` (المنافذ) 🔌

* المسؤول عن التعامل مع العالم الخارجي (HTTP, Controllers, Routes).
* وظيفته الوحيدة: استقبال الطلب، تسليمه للتطبيق، وإرجاع الرد.

### 3. `src/infrastructure` (الأدوات) 🛠️

* يحتوي على التطبيقات الفعلية (Database Connection, Email Service, AI Adapters).
* هنا "تتلوث" يديك بالتفاصيل التقنية (SQL queries, API calls).

---

## 🎨 تشريح الواجهة (Frontend Anatomy)

نتبع **Atomic Design** المعدل ليتناسب مع React.

### 1. `features` (الميزات) 📦

* كل ميزة كبيرة (مثل `Auth`, `Dashboard`, `Lessons`) لها مجلد خاص.
* يحتوي المجلد على: `api`, `components`, `hooks`, `types` الخاصة بهذه الميزة فقط.

### 2. `presentation` (العرض) 🖼️

* **`pages`**: المكونات الكبيرة التي تمثل صفحات كاملة (Organisms).
* **`components/ui`**: المكونات الذرية الغبية (Buttons, Inputs) التي لا تحمل منطقاً خاصاً (Atoms).

### 3. `infrastructure` (البنية التحتية) 📡

* خدمات الاتصال بالخلفية (`Axios`, `Services`).

---

## 🚫 المحظورات الصارمة (The Iron Bans)

1. **يحظر** إنشاء مجلدات عشوائية في الجذر مثل `utils` أو `helpers`. كل دالة مساعدة يجب أن تنتمي لوحدة (Module) أو تذهب إلى `shared/kernel`.
2. **يحظر** استدعاء قاعدة البيانات مباشرة في `Controller`. يجب المرور عبر `Service/UseCase`.
3. **يحظر** كتابة Inline Styles في ملفات `tsx`. استخدم `tailwind` أو `scss modules` فقط.
