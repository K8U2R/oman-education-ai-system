services:
  # 🎨 خدمة الـ Frontend (React/Vite)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: oman_edu_frontend
    restart: always
    ports:
      - "30174:80"
    depends_on:
      - backend

  # ⚙️ خدمة الـ Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: oman_edu_backend
    restart: always
    ports:
      - "30000:30000"
    depends_on:
      - postgres
      - redis
      - database-core
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:SecurePassword123!@postgres:5432/oman_education_db

  # 🗄️ خدمة الـ Database Core
  database-core:
    build:
      context: ./database-core
      dockerfile: Dockerfile
    container_name: oman_edu_db_core
    restart: always
    ports:
      - "3001:3001"
    depends_on:
      - postgres

  # 🐘 خدمة قاعدة البيانات (PostgreSQL)
  postgres:
    image: postgres:15-alpine
    container_name: oman_edu_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: SecurePassword123!
      POSTGRES_DB: oman_education_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # ⚡ خدمة الكاش (Redis)
  redis:
    image: redis:alpine
    container_name: oman_edu_redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data:
