/**
 * Shared Types - أنواع TypeScript المشتركة
 *
 * هذا المجلد يحتوي على أنواع TypeScript المشتركة بين الميزات المختلفة.
 *
 * ## 📋 القواعد:
 *
 * - ✅ ضع هنا فقط الأنواع المستخدمة في أكثر من feature واحدة
 * - ✅ استخدم TypeScript Generics للـ type safety
 * - ✅ اتبع تسمية واضحة وموحدة
 *
 * ## ❌ ما لا يجب وضعه هنا:
 *
 * - ❌ أنواع خاصة بميزة واحدة (ضع في `features/feature-name/types/`)
 * - ❌ أنواع من domain layer (استخدم من `@/domain/types/`)
 *
 * ## 📝 ملاحظات:
 *
 * حالياً لا توجد أنواع مشتركة محددة. سيتم إضافتها عند الحاجة.
 *
 * ### أمثلة على الأنواع التي قد تُضاف هنا:
 *
 * ```typescript
 * // مثال: Async State Type
 * export interface AsyncState<T> {
 *   data: T | null
 *   isLoading: boolean
 *   error: Error | null
 * }
 *
 * // مثال: Pagination Type
 * export interface PaginationParams {
 *   page: number
 *   perPage: number
 * }
 * ```
 *
 * ---
 *
 * **آخر تحديث:** يناير 2026
 */

// سيتم إضافة الأنواع المشتركة هنا عند الحاجة
// export * from './common.types'
