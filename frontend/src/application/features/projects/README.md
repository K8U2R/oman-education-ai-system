# Projects Feature - إدارة المشاريع التعليمية

هذه الميزة ستتيح للمستخدم طلب مشروع تعليمي كامل بالعربية، ويقوم النظام ببنائه تلقائياً (دروس، ملفات، تقارير...).

## 🎯 الهدف

تمكين المستخدمين من:

- طلب مشروع تعليمي كامل بالعربية
- توليد هيكل المشروع تلقائياً باستخدام AI
- تتبع تقدم المشروع
- توليد ملفات التقارير تلقائياً
- إدارة المشاريع المتعددة

## 📁 الهيكلة المخطط لها

```
projects/
├── hooks/
│   ├── useProjectBuilder.ts    # بناء المشروع
│   ├── useProjectProgress.ts   # تتبع التقدم
│   └── index.ts
├── services/
│   ├── project-generator.service.ts  # توليد المشروع (AI Integration)
│   ├── project-manager.service.ts    # إدارة المشاريع
│   └── index.ts
├── store/
│   ├── projectsStore.ts        # State Management
│   └── index.ts
├── types/
│   ├── project.types.ts        # أنواع المشروع
│   └── index.ts
└── README.md
```

## 🚀 الميزات المخطط لها

### 1. بناء المشروع (Project Builder)

- طلب مشروع بالعربية: "أنشئ مشروع تعليمي عن التاريخ العماني"
- توليد هيكل المشروع تلقائياً:
  - الدروس المطلوبة
  - الملفات والموارد
  - التقارير والتقييمات
  - الجدول الزمني

### 2. تتبع التقدم (Progress Tracking)

- عرض تقدم كل مشروع
- إشعارات عند إكمال مراحل
- تقارير تقدم تلقائية

### 3. إدارة المشاريع

- قائمة المشاريع
- تعديل المشاريع
- حذف المشاريع
- مشاركة المشاريع

### 4. تكامل مع الميزات الأخرى

- **office/**: توليد ملفات Excel/Word/PowerPoint للمشروع
- **learning/**: ربط المشاريع بالدروس
- **storage/**: حفظ ملفات المشروع

## 💻 أمثلة استخدام مستقبلية

```typescript
// استخدام hook لبناء مشروع
import { useProjectBuilder } from '@/application/features/projects/hooks'

const MyComponent = () => {
  const { buildProject, isLoading } = useProjectBuilder()

  const handleBuild = async () => {
    await buildProject({
      title: 'مشروع عن التاريخ العماني',
      description: 'مشروع تعليمي شامل عن تاريخ عمان',
      subject: 'التاريخ',
      gradeLevel: 'الصف العاشر',
    })
  }
}
```

```typescript
// استخدام hook لتتبع التقدم
import { useProjectProgress } from '@/application/features/projects/hooks'

const MyComponent = () => {
  const { progress, milestones, completedTasks } = useProjectProgress('project-id')

  return (
    <div>
      <ProgressBar value={progress} />
      <MilestonesList milestones={milestones} />
    </div>
  )
}
```

## 📋 حالة التنفيذ

- [x] إضافة service للتواصل مع Backend API ✅
- [x] إضافة hook لإدارة المشاريع (useProjects) ✅
- [x] إضافة hook لتتبع التقدم (جزء من useProjects) ✅
- [ ] إضافة store لإدارة حالة المشاريع (مستقبلي)
- [x] تكامل مع Backend API ✅
- [x] واجهة المستخدم للمشاريع (ProjectsPage, ProjectDetailPage, ProjectFormPage) ✅

## 🔗 التكاملات المخطط لها

- **AI Service**: لتوليد هيكل المشروع
- **Office Feature**: لتوليد ملفات المشروع
- **Learning Feature**: لربط المشاريع بالدروس
- **Storage Feature**: لحفظ ملفات المشروع
- **Notifications Feature**: لإشعارات التقدم

## 📝 ملاحظات

- هذه الميزة قيد التخطيط
- سيتم البدء في التنفيذ عند الحاجة
- الهيكلة جاهزة للبدء الفوري

---

**آخر تحديث:** يناير 2026  
**الحالة:** ✅ مكتمل - Frontend & Backend
