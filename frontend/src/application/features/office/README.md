# Office Feature - توليد ملفات مكتبية ذكية

توليد ملفات Excel، Word، PowerPoint بناءً على أوامر عربية طبيعية باستخدام الذكاء الاصطناعي.

## 🎯 الهدف

تمكين المستخدمين من:

- توليد ملفات Excel ذكية بأوامر عربية
- توليد ملفات Word بتنسيق احترافي
- توليد عروض PowerPoint تلقائياً
- استخدام AI لفهم الأوامر الطبيعية

## 📁 الهيكلة المخطط لها

```
office/
├── hooks/
│   ├── useExcelGenerator.ts      # توليد Excel
│   ├── useWordGenerator.ts       # توليد Word
│   ├── usePowerPointGenerator.ts # توليد PowerPoint
│   └── index.ts
├── services/
│   ├── office-generator.service.ts  # Service رئيسي (AI + مكتبات)
│   ├── excel.service.ts             # Excel Service
│   ├── word.service.ts              # Word Service
│   ├── powerpoint.service.ts        # PowerPoint Service
│   └── index.ts
├── store/
│   ├── officeStore.ts          # State Management
│   └── index.ts
├── types/
│   ├── office.types.ts         # أنواع الملفات
│   └── index.ts
└── README.md
```

## 🚀 الميزات المخطط لها

### 1. توليد Excel

- إنشاء جداول بيانات ذكية
- إضافة معادلات تلقائياً
- تنسيق احترافي
- أمثلة:
  - "أنشئ جدول درجات لـ 30 طالب مع معادلات تلقائية"
  - "أنشئ جدول حضور وغياب للفصل الدراسي"

### 2. توليد Word

- إنشاء مستندات بتنسيق احترافي
- إضافة محتوى تلقائياً
- تنسيق الجداول والقوائم
- أمثلة:
  - "أنشئ تقرير وورد عن درس التاريخ"
  - "أنشئ خطة درس بتنسيق احترافي"

### 3. توليد PowerPoint

- إنشاء عروض تقديمية تلقائياً
- إضافة شرائح ذكية
- تنسيق احترافي
- أمثلة:
  - "أنشئ عرض تقديمي عن الرياضيات للصف السابع"
  - "أنشئ عرض عن تاريخ عمان مع صور"

## 💻 أمثلة استخدام مستقبلية

```typescript
// استخدام hook لتوليد Excel
import { useExcelGenerator } from '@/application/features/office/hooks'

const MyComponent = () => {
  const { generateExcel, isLoading } = useExcelGenerator()

  const handleGenerate = async () => {
    await generateExcel({
      command: 'أنشئ جدول درجات لـ 30 طالب مع معادلات تلقائية',
      filename: 'جدول_الدرجات.xlsx',
    })
  }
}
```

```typescript
// استخدام hook لتوليد Word
import { useWordGenerator } from '@/application/features/office/hooks'

const MyComponent = () => {
  const { generateWord, isLoading } = useWordGenerator()

  const handleGenerate = async () => {
    await generateWord({
      command: 'أنشئ تقرير وورد عن درس التاريخ',
      filename: 'تقرير_التاريخ.docx',
    })
  }
}
```

```typescript
// استخدام hook لتوليد PowerPoint
import { usePowerPointGenerator } from '@/application/features/office/hooks'

const MyComponent = () => {
  const { generatePowerPoint, isLoading } = usePowerPointGenerator()

  const handleGenerate = async () => {
    await generatePowerPoint({
      command: 'أنشئ عرض تقديمي عن الرياضيات للصف السابع',
      filename: 'عرض_الرياضيات.pptx',
      slides: 10,
    })
  }
}
```

## 🔧 المكتبات المخطط استخدامها

- **Excel**: `xlsx` أو `exceljs`
- **Word**: `docx`
- **PowerPoint**: `pptxgenjs` أو `officegen`

## 🤖 تكامل AI

- استخدام AI لفهم الأوامر العربية الطبيعية
- توليد هيكل الملف تلقائياً
- إضافة محتوى ذكي
- تنسيق احترافي

## 📋 حالة التنفيذ

- [x] إضافة service للتواصل مع Backend API ✅
- [x] إضافة hook لاستخدام المولدات (useOffice) ✅
- [x] تكامل مع Backend API ✅
- [x] واجهة المستخدم (OfficeGeneratorPage) ✅
- [ ] إضافة store لإدارة الحالة (مستقبلي)

## 🔗 التكاملات المخطط لها

- **AI Service**: لفهم الأوامر وتوليد المحتوى
- **Storage Feature**: لحفظ الملفات المولدة
- **Projects Feature**: لتوليد ملفات المشاريع
- **Learning Feature**: لتوليد ملفات الدروس

## 📝 ملاحظات

- هذه الميزة قيد التخطيط
- سيتم البدء في التنفيذ عند الحاجة
- الهيكلة جاهزة للبدء الفوري
- ستحتاج إلى مكتبات خارجية لتوليد الملفات

---

**آخر تحديث:** يناير 2026  
**الحالة:** ✅ مكتمل - Frontend & Backend
