# API Services - خدمات التواصل مع Backend

هذا المجلد يحتوي على الخدمات المسؤولة عن التواصل مع واجهات برمجة التطبيقات (APIs) وإدارة الطلبات.

## 📁 محتويات المجلد

- `api-client.ts`: عميل API الرئيسي لإجراء طلبات HTTP
- `api-client.refactored.ts`: النسخة المحسنة والمطورة من عميل API
- `request-queue.ts`: إدارة طوابير الطلبات والتعامل مع الطلبات في حالة عدم وجود اتصال
- `types.ts`: أنواع TypeScript الخاصة بطلبات واستجابات API
- `index.ts`: نقطة التصدير المركزية للخدمات

## 🎯 الوظائف الرئيسية

1. **إدارة الطلبات (Requests)**: تنفيذ عمليات GET, POST, PUT, DELETE.
2. **المعترضات (Interceptors)**: تتبع الطلبات والاستجابات لإضافة Tokens أو معالجة الأخطاء.
3. **طابور الطلبات (Queue)**: ضمان تنفيذ الطلبات بالترتيب الصحيح وإعادة المحاولة عند الفشل.
4. **التوحيد (Standardization)**: توحيد شكل الاستجابات ومعالجة الأخطاء عبر التطبيق.

## 📦 الاستخدام

```typescript
import { apiClient } from '@/infrastructure/services'

// مثال لطلب بيانات
const data = await apiClient.get('/users/profile')
```
