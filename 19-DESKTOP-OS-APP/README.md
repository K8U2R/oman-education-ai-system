# 🖥️ تطبيق سطح المكتب - نظام تشغيل موحد
# Desktop OS Application - Unified Operating System

## 📋 نظرة عامة

تطبيق سطح المكتب المتكامل لإدارة وتشغيل جميع أنظمة مشروع التعليم الذكي العُماني من واجهة واحدة موحدة.

## ✨ الميزات

### 🎮 مركز التحكم
- تشغيل/إيقاف جميع الخدمات (Backend, Frontend, Integration)
- إعادة تشغيل الخدمات تلقائياً
- مراقبة حالة الخدمات في الوقت الفعلي

### 📊 لوحة التحكم
- عرض حالة جميع الخدمات
- مقاييس النظام (CPU, RAM, Disk, Network)
- معلومات النظام والخدمات

### 📝 عارض السجلات
- عرض سجلات جميع الخدمات
- تحديث تلقائي
- تصفية حسب الخدمة

### 🔔 نظام التنبيهات
- تنبيهات فورية عند حدوث مشاكل
- مستويات مختلفة (Info, Warning, Error, Critical)
- إشعارات في أيقونة النظام

### 🎨 واجهة مستخدم حديثة
- دعم الوضع الليلي والنهاري
- تصميم احترافي وحديث
- دعم اللغة العربية والإنجليزية

## 🚀 التثبيت والتشغيل

### المتطلبات
```bash
pip install -r packaging/requirements.txt
```

### التشغيل
```bash
python app-shell/main.py
```

### بناء التطبيق
```bash
cd packaging
pyinstaller build.spec
```

## 📁 الهيكل

```
19-DESKTOP-OS-APP/
├── app-shell/          # غلاف التطبيق
├── orchestrator/       # منسق التشغيل
├── ui_modules/         # وحدات الواجهة
├── integrations/      # التكاملات
├── monitoring/        # المراقبة
└── packaging/         # التعبئة
```

## 🔧 الإعدادات

الإعدادات محفوظة في `config/app_config.json` ويمكن تعديلها من داخل التطبيق.

## 📝 ملاحظات

- التطبيق يتطلب Python 3.8+
- PySide6 مطلوب لواجهة المستخدم
- جميع الخدمات يجب أن تكون قابلة للوصول عبر HTTP

## 📑 مراحل البناء والربط

لمعرفة مراحل التطوير وكيفية ربط تطبيق سطح المكتب بباقي الأنظمة (01–19)، راجع:

- `BUILD_PHASES.md` – جدول المراحل البنائية وربط الأنظمة.
- `DEVELOPMENT_PLAN.md` – خطة التطوير التفصيلية.
- `PROGRESS_TRACKER.md` – تتبع التقدم اليومي.

## 🆘 الدعم

للحصول على المساعدة، راجع الوثائق في `14-DOCUMENTATION/`.

