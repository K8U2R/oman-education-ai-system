# 🚀 تعليمات رفع المشروع إلى GitHub

## ✅ تم إكمال الخطوات التالية:

1. ✅ إزالة المفاتيح الحساسة من `create_env_files.py`
2. ✅ إضافة commit للتغييرات الأمنية
3. ✅ إعداد remote مع حسابك: `K8U2R`

---

## 📋 الخطوة المتبقية: إنشاء المستودع على GitHub

### 1. إنشاء المستودع الجديد

1. اذهب إلى: https://github.com/new
2. املأ التفاصيل:
   - **Repository name:** `oman-education-ai-system`
   - **Description:** `نظام ذكي عربي للتعلم والبناء العملي - AI-powered Arabic Education System`
   - **Visibility:** اختر **Public** أو **Private**
   - ⚠️ **لا تضع علامة على** "Initialize with README" (لأن لدينا README.md بالفعل)
   - ⚠️ **لا تضع علامة على** "Add .gitignore" (لأن لدينا .gitignore بالفعل)
3. اضغط **"Create repository"**

---

### 2. رفع المشروع

بعد إنشاء المستودع، شغّل الأمر التالي:

```powershell
git push -u origin main
```

**إذا طُلب منك اسم المستخدم وكلمة المرور:**

#### الطريقة 1: استخدام Personal Access Token (موصى به)

1. اذهب إلى: https://github.com/settings/tokens
2. اضغط **"Generate new token"** → **"Generate new token (classic)"**
3. املأ التفاصيل:
   - **Note:** `oman-education-ai-system`
   - **Expiration:** اختر المدة المناسبة
   - **Select scopes:** ✅ **repo** (كامل)
4. اضغط **"Generate token"**
5. **انسخ Token فوراً** (لن تتمكن من رؤيته مرة أخرى!)
6. عند طلب كلمة المرور، استخدم **Token** بدلاً من كلمة المرور

#### الطريقة 2: استخدام SSH (بديل)

```powershell
# تغيير remote إلى SSH
git remote set-url origin git@github.com:K8U2R/oman-education-ai-system.git

# رفع المشروع
git push -u origin main
```

---

## 🔒 الأمان - معلومات مهمة

### ✅ تم حماية المفاتيح الحساسة:

- ✅ تم إزالة جميع المفاتيح الحقيقية من `create_env_files.py`
- ✅ ملفات `.env` موجودة في `.gitignore` ولن تُرفع
- ✅ تم استبدال المفاتيح بقيم placeholder آمنة

### ⚠️ إذا رفعت المفاتيح بالخطأ في الماضي:

إذا كان المشروع يحتوي على commits قديمة تحتوي على المفاتيح الحساسة:

1. **غيّر جميع المفاتيح فوراً:**
   - Gemini API Key: https://aistudio.google.com/app/apikey
   - Google OAuth: https://console.cloud.google.com/apis/credentials
   - OpenAI API Key: https://platform.openai.com/api-keys

2. **استخدم Git History Rewrite (للمستودعات الخاصة فقط):**
   ```powershell
   # تحذير: هذا سيغير التاريخ - استخدم فقط للمستودعات الخاصة!
   git filter-branch --force --index-filter "git rm --cached --ignore-unmatch create_env_files.py" --prune-empty --tag-name-filter cat -- --all
   git push origin --force --all
   ```

---

## 📝 بعد الرفع الناجح

المشروع سيكون متاحاً على:
**https://github.com/K8U2R/oman-education-ai-system**

---

## 🆘 حل المشاكل

### المشكلة: `Authentication failed`

**الحل:**
- استخدم Personal Access Token بدلاً من كلمة المرور
- أو استخدم SSH

### المشكلة: `Repository not found`

**الحل:**
- تأكد من إنشاء المستودع على GitHub أولاً
- تأكد من اسم المستخدم: `K8U2R`
- تأكد من اسم المستودع: `oman-education-ai-system`

### المشكلة: `error: failed to push some refs`

**الحل:**
```powershell
# سحب التغييرات من GitHub أولاً
git pull origin main --allow-unrelated-histories

# ثم رفع التغييرات
git push -u origin main
```

---

## ✅ Checklist

- [ ] تم إنشاء المستودع على GitHub
- [ ] تم إزالة المفاتيح الحساسة من `create_env_files.py`
- [ ] تم إعداد remote: `git remote -v`
- [ ] تم رفع المشروع: `git push -u origin main`
- [ ] تم التحقق من أن الملفات الحساسة غير موجودة على GitHub

---

**🎉 بعد اكتمال الرفع، سيكون المشروع جاهزاً للاستخدام والتطوير!**

