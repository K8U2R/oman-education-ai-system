# 🦅 تقرير مسح الهيكل المعماري (ARCHITECTURE_AUDIT.md)

| المرجع: .ai_governance/LAWS.md (Law 10) | التاريخ: 2026-01-22 | الحالة: مسودة مراجعة |
|---|---|---|

## 📊 مصفوفة مخاطر التضخم (Bloat Risk Matrix)

| المجلد (Directory) | الحالة الحالية (Current State) | احتمالية التضخم (Bloat Risk) 📈 | الإجراء التصحيحي المطلوب (Refactoring Action) 🛠️ |
| :--- | :--- | :--- | :--- |
| `src/presentation/api/handlers` | مجلدات فرعية حسب النطاق، لكنها تفتقر للتنظيم الداخلي الدقيق. عدد المعالجات في ازدياد. | **عالية (High) 🚨** | تعزيز التقسيم العنقودي (Cluster-based handlers) وتوحيد أنماط الاستجابة. |
| `src/domain/entities` | ملفات مسطحة في الجذر (4 ملفات حالياً). ستزدحم عند إضافة Gamification. | **متوسطة (Medium) ⚠️** | تجميع الكيانات في عناقيد (مثلاً: `auth/`, `user/`, `content/`). |
| `src/presentation/api/dto` | تقسيم مبدئي لـ 7 نطاقات، لكن الملفات تنمو بسرعة داخل كل نطاق. | **عالية (High) 🚨** | عزل الـ DTOs لكل Domain Controller وفصل الـ Request عن الـ Response. |
| `src/application/services` | مجلدات فرعية لـ 20 نطاقاً، لكنها تعاني من تداخل المهام (Logic Mixing). | **عالية (High) 🚨** | تطبيق Single Responsibility وفصل الـ Orchestration Services عن الـ Core Services. |
| `src/infrastructure/database/scripts` | سكربتات تهجئة موزعة بشكل جيد. | **منخفضة (Low) ✅** | الاحتفاظ بنمط التسمية الحالي مع إضافة توثيق لكل Migration. |
| `src/domain/types` | **كارثية!** 33 ملفاً في مجلد واحد مسطح. هذا هو أكبر "بؤر التضخم" حالياً. | **كارثية (Critical) 🔥** | إعادة توزيع الأنواع على مجلدات النطاقات الخاصة بها (Distributed Types Map). |
| `src/presentation/api/middleware` | مجلدات فرعية (4) مع بعض الملفات المتناثرة في الجذر. | **متوسطة (Medium) ⚠️** | نقل كافة الميدل وير إلى العناقيد المناسبة (Security, Monitoring, Traffic). |

## 🚨 بؤرة الانفجار الحالية (The Red Zone)

يعد مجلد `src/domain/types` هو الخطر الأكبر حالياً؛ حيث يحتوي على 33 ملفاً متنوعاً (من `auth.types.ts` إلى `websocket.types.ts`) في مستوى واحد. هذا الهيكل يجعل من الصعب جداً تتبع الاعتماديات ويزيد من وقت البحث عن الأخطاء.

## 🎯 التوصية المعمارية

يجب البدء فوراً في تنفيذ **"المجموعة السيادية للأنواع" (Sovereign Types Map)** عن طريق نقل كل ملف `.types.ts` إلى مجلد النطاق الذي يمثله (مثلاً: `domain/types/auth/*.ts`) لضمان التوافق مع Law 1 (Modularity).

---
*تم إعداد هذا التقرير بواسطة Antigravity AI* 🦅
