# ============================================
# Database Core Service - Environment Variables
# ============================================
# 
# انسخ هذا الملف إلى .env واملأ القيم الفعلية
# cp env.example .env
#
# ⚠️ مهم: لا تشارك ملف .env أبداً - يحتوي على معلومات حساسة

# ============================================
# Supabase Configuration (معطل افتراضياً - للاستخدام في الإنتاج فقط)
# ============================================
# ⚠️ ملاحظة: Supabase معطل افتراضياً للتنمية المحلية
# لتفعيل Supabase، قم بتعيين ENABLE_SUPABASE=true وإضافة المتغيرات التالية

# تفعيل Supabase (افتراضي: false - معطل)
# ENABLE_SUPABASE=false

# URL الخاص بمشروع Supabase
# يمكنك العثور عليه في Supabase Dashboard > Settings > API
# SUPABASE_URL=https://your-project-id.supabase.co

# Service Role Key - مفتاح  الكاملة
# ⚠️ مهم جداً: لا تشارك هذا المفتاح أبداً - يستخدم فقط في Backend
# يمكنك العثور عليه في Supabase Dashboard > Settings > API > service_role key
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# Server Configuration
# ============================================
# المنفذ الذي ستعمل عليه الخدمة
PORT=3001

# بيئة التشغيل: development | production | test
NODE_ENV=development

# ============================================
# Logging Configuration
# ============================================
# مستوى التسجيل: error | warn | info | debug
LOG_LEVEL=info

# ============================================
# Database Routing Configuration (اختياري)
# ============================================
# استراتيجية التوجيه: primary | round-robin | load-balance
# افتراضي: primary
# DATABASE_ROUTING_STRATEGY=primary

# معرف الاتصال الأساسي (افتراضي: postgresql-internal)
# DATABASE_PRIMARY_CONNECTION_ID=postgresql-internal

# معرفات الاتصالات الاحتياطية (مفصولة بفواصل)
# DATABASE_FALLBACK_CONNECTION_IDS=postgresql-internal,mongodb-internal

# ============================================
# PostgreSQL Configuration (مطلوب - الاتصال الأساسي للتنمية المحلية)
# ============================================
# PostgreSQL هو الاتصال الأساسي افتراضياً للتنمية المحلية
# عند النشر للإنتاج، يمكنك إعادة تفعيل Supabase عبر ENABLE_SUPABASE=true

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=oman_education_db
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=postgres

# حجم Connection Pool (افتراضي: 20)
# POSTGRES_POOL_SIZE=20

# مهلة الاتصال بالمللي ثانية (افتراضي: 30000)
# POSTGRES_TIMEOUT=30000

# ============================================
# Redis Configuration (اختياري - للـ Caching)
# ============================================
# Redis Host (افتراضي: localhost)
# REDIS_HOST=localhost

# Redis Port (افتراضي: 6379)
# REDIS_PORT=6379

# Redis Password (اختياري)
# REDIS_PASSWORD=your_redis_password

# Redis Database Number (افتراضي: 0)
# REDIS_DB=0

# Redis Connection String (بديل للـ host/port/password)
# REDIS_URL=redis://localhost:6379

# Redis Cache TTL بالمللي ثانية (افتراضي: 5 دقائق)
# REDIS_TTL=300000

# ============================================
# Authentication Service Configuration (اختياري)
# ============================================
# رابط خدمة المصادقة (للتكامل مع Backend Service)
# AUTH_SERVICE_URL=http://localhost:3000
# AUTH_SERVICE_API_KEY=your_auth_service_api_key

# ============================================
# Policy Configuration (اختياري)
# ============================================
# السماح افتراضياً (افتراضي: true)
# POLICY_DEFAULT_ALLOW=true

# الوضع الصارم (افتراضي: false)
# POLICY_STRICT_MODE=false

# ============================================
# CORS Configuration (اختياري)
# ============================================
# CORS_ORIGIN=http://localhost:3000,http://localhost:4173

# ============================================
# Backup Configuration (اختياري)
# ============================================
# مجلد حفظ النسخ الاحتياطية (افتراضي: ./backups)
# BACKUP_DIRECTORY=./backups

# عدد أيام الاحتفاظ بالنسخ الاحتياطية (افتراضي: 30 يوم)
# BACKUP_RETENTION_DAYS=30

# تفعيل الضغط (افتراضي: true)
# BACKUP_COMPRESSION=true

# تفعيل النسخ الاحتياطي التدريجي (افتراضي: true)
# BACKUP_INCREMENTAL=true

# تفعيل الجدولة التلقائية (افتراضي: false)
# BACKUP_SCHEDULE_ENABLED=false

# نوع الجدولة: hourly | daily | weekly (افتراضي: daily)
# BACKUP_SCHEDULE_INTERVAL=daily

# وقت الجدولة (مثال: 02:00)
# BACKUP_SCHEDULE_TIME=02:00

# ============================================
# Notes
# ============================================
# 1. المتغيرات المطلوبة للتنمية المحلية: POSTGRES_HOST, POSTGRES_DATABASE, POSTGRES_USERNAME, POSTGRES_PASSWORD
# 2. Supabase معطل افتراضياً - لتفعيله: ENABLE_SUPABASE=true
# 3. في وضع التطوير المحلي، استخدم PostgreSQL (الافتراضي)
# 4. عند النشر للإنتاج، يمكنك:
#    - إما استخدام PostgreSQL مباشرة (مستحسن)
#    - أو تفعيل Supabase عبر ENABLE_SUPABASE=true وإضافة SUPABASE_URL و SUPABASE_SERVICE_ROLE_KEY
# 5. للحصول على SUPABASE_URL و SUPABASE_SERVICE_ROLE_KEY (إذا كنت تريد استخدام Supabase):
#    - اذهب إلى Supabase Dashboard
#    - Settings > API
#    - Project URL = SUPABASE_URL
#    - service_role key = SUPABASE_SERVICE_ROLE_KEY
#    - ⚠️ لا تشارك service_role key أبداً - صلاحيات كاملة!

