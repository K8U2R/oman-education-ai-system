# HTTP Services - خدمات HTTP

هذا المجلد يحتوي على البنية التحتية الأساسية لطلبات HTTP، والتي يعتمد عليها عميل API.

## 📁 محتويات المجلد

- `http-client.ts`: التطبيق الأساسي لعميل HTTP
- `http-client.factory.ts`: مصنع لإنشاء مثيلات مختلفة من عملاء HTTP
- `index.ts`: نقطة التصدير المركزية

## 🎯 الوظائف الرئيسية

1. **تكوين العميل (Configuration)**: إعداد القيم الافتراضية للطلبات (Timeout, Base URL).
2. **المرونة (Flexibility)**: إمكانية إنشاء عملاء مخصصين لأغراض مختلفة.
3. **طبقة أساسية (Core Layer)**: توفير الأدوات التقنية التي يحتاجها `api-client`.

## 📦 الاستخدام

```typescript
import { httpClient } from '@/infrastructure/services'

// استخدام العميل الأساسي
const response = await httpClient.request({
  url: '/health',
  method: 'GET',
})
```
