# 🏛️ دستور الحوكمة السيادية للمشروع (Sovereign Rules 2026)

> "النظام هو حجر الزاوية للبقاء." - المعيار الفني السيادي

يحدد هذا المستند القوانين غير القابلة للتفاوض لتطوير الواجهات الأمامية في **نظام عُمان للتعليم الذكي** (الإصدار السيادي 2026). وُجدت هذه القوانين لمنع التشتت البرمجي، وضمان التوسع اللانهائي، والحفاظ على "السيادة السائلة".

---

## 1. 📦 قانون التغليف السيادي (Encapsulation Law)

**القاعدة:** يُمنع منعاً باتاً إنشاء صفحة (Page) أو مكون (Component) دون ملف أنماط (Style) مرافق يحمل نفس الاسم تماماً في نفس المجلد.

**الهيكلية المطلوبة:**
```text
اسم_المكون/
├── components/      # المكونات الفرعية السيادية
├── core/            # المنطق والأمان (Hooks, Types)
├── [Module].tsx     # واجهة العرض الرئيسية
├── [Module].module.scss  # التنسيق السائل (السيادة البصرية)
└── [Module].index.ts     # بوابة الدخول
```

**السبب:** منع "تبعثر المجلدات" وجعل "كشف الأخطاء من جحرها المختفي" أمراً فورياً. كل مكون يجب أن يكون وحدة سيادية مستقلة بذاتها.

---

## 2. 📛 قانون تسمية السيادة الكوني (Universal Naming Law)

**القاعدة:** يُمنع استخدام أسماء ملفات غامضة مثل `index.ts` أو `styles.scss` بشكل مجرد.

**البروتوكول:**
1.  **بوابات الدخول (Indexes):** يجب تسمية ملفات المدخل بـ `[ModuleName].index.ts` لضمان البحث الذكي السريع (Fuzzy Search Optimization).
2.  **فصل السلطات (Separation of Concerns):**
    *   المنطق (Logic): يوضع حصراً في `core/[Module].hooks.ts`.
    *   الأنواع (Types): توضع حصراً في `core/[Module].types.ts`.
    *   العرض (View): يقتصر `[Module].tsx` على التركيب البصري فقط (JSX).

**الهدف:** أن يعرف المطور محتوى الملف من اسمه دون فتحه، مما يسرع عمليات التدقيق (Audit) بنسبة 400%.

---

## 3. 🧩 هيكلية المجلدات السيادية (Standardized Folder Structure)

**القاعدة:** يجب تطبيق الهيكل التالي بصرامة على جميع الأقسام (Sidebar, Dashboard, Profile...):

```text
FolderName/ (اسم القسم الرئيسي)
├── components/          # المكونات الفرعية (Sub-components)
│   └── SubItem/         # كل مكون فرعي في مجلد خاص
│       ├── SubItem.tsx
│       └── SubItem.module.scss
│
├── core/                # العقل المدبر (Logic & Types)
│   ├── [Module].hooks.ts
│   ├── [Module].types.ts
│   └── [Module].utils.ts
│
├── [Module].tsx         # واجهة المستخدم السيادية
├── [Module].module.scss # قانون التنسيق المحلي
└── [Module].index.ts    # نقطة التصدير العامة
```

---

## 4. 🌍 قانون "توطين الأنماط" (Style Localization)

**القاعدة:** يُمنع وجود مجلد `styles/` عام للأقسام. يجب أن "يسكن" كل ملف تنسيق بجانب المكون الذي يخدمه مباشرة.

**الاستثناء:** المجلد `src/styles` العام يقتصر فقط على:
1.  الأساسيات (`base/`): الخطوط، إعادة التعيين (Reset).
2.  التجريد (`abstracts/`): متغيرات السيادة السائلة (`liquid-variables`).
3.  المحركات (`engine/`): مولدات الثيمات (Theme Generators).

---

## 5. 💧 معايير السيادة السائلة (Liquid Standards)

**الاستيراد الإلزامي:** يجب أن يبدأ كل ملف `.scss` محلي بالسطر التالي:
```scss
@use '@/styles/abstracts/liquid-variables' as liquid;
@use '@/styles/abstracts/mixins' as *;
```

**المحرمات البرمجية:** يُمنع استخدام قيم البكسل الثابتة (مثل: `width: 300px`).

**المتطلبات:**
- استخدام وحدة `rem` للوحدات الأساسية.
- استخدام معادلات `clamp()` أو المتغيرات السائلة الجاهزة مثل `$spacing-fluid-*` لجميع الأبعاد الاستجابية.

---

## 6. 🤝 ميثاق رضا المستخدم (User-Satisfaction Charter)

**أولاً: حالات التحميل (Skeleton Loading):**
إلزامية توفير حالة "تحميل هيكلي" (Skeleton State) لكل مكون يعتمد على البيانات. لا يُسمح بظهور مساحات فارغة أثناء جلب البيانات.

**ثانياً: الانسياب الاتجاهي (Logical Properties):**
لضمان السيادة اللغوية (عربي/إنجليزي)، يُمنع استخدام الاتجاهات الفيزيائية (`left`, `right`, `margin-left`). البديل الإلزامي هو الخصائص المنطقية:
- `margin-inline-start` بدلاً من `margin-left`.
- `padding-inline-end` بدلاً من `padding-right`.
- `inset-inline-start` بدلاً من `left`.

---

## 7. 🔗 بروتوكول الربط المحكم (Binding Protocol)

**القاعدة:** يجب على ملف المنطق (`.tsx`) استيراد شريكه التنسيقي بشكل صريح لضمان التغليف الكامل.

**الهدف:** التأكد من أن حذف مجلد المكون يؤدي إلى حذف كل أثره من النظام، دون ترك "أنماط شبحية" تؤثر على الأداء.

---

## 8. 🗣️ قانون السيادة اللغوية (Language Sovereignty Law)

**القاعدة:** يُمنع منعاً باتاً كتابة أي نص (عربي أو إنجليزي) بشكل مباشر (Hardcoded) في ملفات الـ `tsx`.

**الإجراء:** يجب استخدام دالة الترجمة `t()` حصراً، وربطها بمفاتيح الترجمة المنظمة في:
- `apps/frontend/src/application/shared/i18n/locales/ar/common.json`
- `apps/frontend/src/application/shared/i18n/locales/en/common.json`

---

## 9. 🤝 بروتوكول الالتزام المستقبلي (Future Commitment Protocol)

**التعهد:** عند بدء أي مهمة برمجية جديدة، يجب مراجعة هذا المستند لضمان أن الكود الناتج يحترم السيادة التصميمية واللغوية للنظام.

---

## 10. 🎨 بروتوكول تسمية المحرك البصري (Visual Engine Naming Protocol)

**القاعدة:** يجب أن تكون أسماء ملفات الأنماط الأساسية (`src/styles/abstracts`) وصفية دقيقة لوظيفتها، ويُمنع استخدام الأسماء العامة الغامضة.

**التحويل الإلزامي:**
1. `_tokens.scss` ⬅️ **`_foundation-colors.scss`**: يحتوي حصراً على متغيرات الألوان والدرجات.
2. `_variables.scss` ⬅️ **`_foundation-typography.scss`**: يحتوي حصراً على أوزان الخطوط وأحجامها الأساسية.
3. `_liquid-variables.scss` ⬅️ **`_engine-fluid-metrics.scss`**: يحتوي على معادلات `clamp()` والوحدات السائلة.
4. `_mixins.scss` ⬅️ **`_engine-helpers.scss`**: يحتوي على أدوات الـ RTL والـ Responsive.

**الهدف:** القضاء على الغموض الهيكلي وجعل الغرض من كل ملف واضحاً من اسمه.

---

## 11. 🚫 قانون حظر الاتجاهات الفيزيائية (Physical Directions Ban)

**القاعدة:** في المجلد `src/styles/base/`، يُمنع منعاً باتاً استخدام أي خصائص CSS تعتمد على الاتجاهات الفيزيائية (`left`, `right`).

**البديل السيادي:** يجب استخدام الخصائص المنطقية فقط (`inline-start`, `inline-end`) لضمان دعم ثنائي الاتجاه (Bi-directional) من القاع.

---

## 12. 🧹 قانون التطهير الهيكلي (Structural Purge Law)

**القاعدة:** تُعتبر المجلدات التالية داخل `src/styles` "محرمة برمجياً" ويجب حذفها فوراً:
*   `styles/pages/`
*   `styles/layouts/`
*   `styles/components/`
*   `styles/themes/`

**الإجراء:** يجب توزيع محتويات هذه المجلدات على الأقسام السيادية الخاصة بها (Co-location) أو نقلها إلى `src/styles/engine` إذا كانت عامة جداً.

---

**توقيع:** Antigravity، حارس الكود.
**بإشراف:** ناصر، مهندس السيادة.

---

## 13. 🔬 قانون التفكيك الوظيفي (Functional Decomposition Law)

**القاعدة:** أي ملف JSX (صفحة أو مكون) يتجاوز **100 سطر** يُعتبر "ملوثاً" ويجب تفكيكه فوراً.

**البروتوكول:**
1.  **التحليل:** تحديد الأجزاء المنطقية (Logical Sections) داخل الملف (مثل: Header, Stats, List).
2.  **الاستخراج:** نقل كل جزء إلى مجلد `components/` المحلي الخاص بالمكون الأم.
3.  **التجميع:** استيراد المكونات الفرعية ليبقى الملف الأم نظيفاً ومقروءاً (أقل من 100 سطر).
4.  **الهدف:** "كل ملف يروي قصة واحدة فقط". تقليل التعقيد المعرفي (Cognitive Load).