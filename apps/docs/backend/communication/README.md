# وحدة التواصل (Communication Module) 📡

تُعد **وحدة التواصل** نظاماً مركزياً لإدارة جميع قنوات الاتصال الصادرة والواردة في "نظام التعليم الذكي العماني". تم تصميمها باستخدام **هيكلية القنوات (Channel-Based Architecture)** لدعم التوسع وسهولة دمج مزودين جدد (مثل WhatsApp، إشعارات الدفع) دون تعديل المنطق الأساسي.

## 📂 هيكلية الدليل

تقع الوحدة في المسار: `apps/backend/src/presentation/api/handlers/communication/`.

```
communication/
├── index.ts            # تصديرات واجهة برمجة التطبيقات العامة
├── shared/             # الأصناف الأساسية والأدوات المساعدة
│   └── BaseCommunicationHandler.ts
└── channels/           # تطبيقات القنوات المحددة
    ├── notification/   # الإشعارات داخل التطبيق (SSE + قاعدة بيانات)
    ├── email/          # خدمة البريد الإلكتروني (نموذج)
    └── sms/            # خدمة الرسائل النصية (نموذج)
```

## 🚀 الميزات الرئيسية

*   **معالج أساسي موحد**: ترث جميع القنوات من `BaseCommunicationHandler` لضمان تنسيق ردود موحد (`sent`, `queued`).
*   **تحقق صارم**: تستخدم جميع النقاط النهائية مخططات **Zod** للتحقق من المدخلات.
*   **قنوات مفصولة**: إضافة قناة جديدة (مثل `whatsapp`) تتطلب فقط إنشاء مجلد جديد في `channels/` وتمديد الصنف الأساسي.
*   **دعم الوقت الفعلي**: تدعم قناة `notification` الأحداث المرسلة من الخادم (SSE) للتحديثات المباشرة.

## 📖 أقسام التوثيق

1.  [الهيكلية والمفاهيم](./architecture.md): فهم نمط القنوات.
2.  [دليل المعالج الأساسي](./handlers.md): استخدام `BaseCommunicationHandler`.
3.  [القنوات](./channels/README.md): تفاصيل القنوات المحددة.
    *   [الإشعارات](./channels/notification.md)
    *   [البريد الإلكتروني](./channels/email.md)
    *   [الرسائل النصية](./channels/sms.md)
