# قناة الرسائل النصية (SMS Channel) 📱

تتعامل قناة الرسائل النصية مع خدمات الرسائل القصيرة. تم تصميمها للعمل مع مزودين مثل Twilio أو البوابات المحلية.

## 📂 الموقع
`apps/backend/src/presentation/api/handlers/communication/channels/sms/`

## مرجع واجهة برمجة التطبيقات (API Reference)

### 1. إرسال رسالة نصية
يرسل رسالة نصية إلى رقم هاتف.

*   **نقطة النهاية**: `POST /api/v1/communication/sms/send`
*   **الجسم (Body)**:
    ```json
    {
      "to": "+96899999999",
      "body": "رمز التحقق الخاص بك هو 1234."
    }
    ```
*   **الاستجابة**: `202 Accepted` (عادة ما تكون في الطابور Queued).

## تفاصيل التنفيذ

### نموذج المعالج (`SmsHandler.ts`)
يقوم المعالج بقبول الطلب ووضعه في الطابور.

```typescript
// المنطق الحالي للنموذج
const payload = sendSmsSchema.parse(req.body);
// TODO: استدعاء this.smsService.send(payload)
this.queued(res, "mock-job-id-456");
```

### العمل المستقبلي
1.  دمج `twilio` SDK في `MessagingService`.
2.  تنفيذ نقطة نهاية webhook لتحديثات حالة التسليم.
