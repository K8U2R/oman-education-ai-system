# 🚀 دليل البدء السريع - Frontend

## 📋 المتطلبات

- Node.js 18+ 
- npm أو yarn أو pnpm
- Git

## ⚡ البدء السريع

### 1. تثبيت التبعيات

```bash
cd 03-WEB-INTERFACE/frontend
npm install
```

### 2. إعداد متغيرات البيئة

#### الطريقة 1: استخدام السكريبت (موصى به)

```bash
# من المجلد الرئيسي للمشروع
python create_env_files.py
```

#### الطريقة 2: يدوياً

```bash
# انسخ ملف المثال
cp .env.example .env

# عدّل القيم حسب الحاجة
# خاصة VITE_API_BASE_URL
```

### 3. تشغيل التطبيق

```bash
npm run dev
```

التطبيق سيعمل على: `http://localhost:3000`

---

## 🛠️ الأوامر المتاحة

### التطوير

```bash
npm run dev          # تشغيل في وضع التطوير
npm run build        # بناء للإنتاج
npm run preview      # معاينة البناء
```

### الجودة

```bash
npm run lint         # فحص الكود
npm run lint:fix     # إصلاح أخطاء الكود
npm run format       # تنسيق الكود
npm run type-check   # فحص الأنواع
```

### الاختبارات

```bash
npm run test         # اختبارات الوحدات
npm run test:ui      # واجهة اختبارات
npm run test:coverage # تغطية الاختبارات
npm run test:e2e     # اختبارات E2E
```

---

## 📁 هيكل المشروع

```
frontend/
├── public/          # ملفات عامة (PWA, SEO)
├── src/
│   ├── components/  # مكونات عامة
│   ├── core/        # النواة الأساسية
│   ├── modules/     # الوحدات الرئيسية
│   ├── hooks/       # Custom Hooks
│   ├── services/    # الخدمات
│   ├── store/       # State Management
│   ├── utils/       # أدوات مساعدة
│   └── config/      # التكوينات
└── package.json
```

---

## 🔧 الإعدادات المهمة

### PWA

التطبيق يدعم PWA تلقائياً. بعد البناء:

```bash
npm run build
npm run preview
```

افتح في المتصفح وستجد خيار "تثبيت التطبيق".

### Sentry (اختياري)

لتفعيل مراقبة الأخطاء في الإنتاج:

1. احصل على DSN من [sentry.io](https://sentry.io)
2. أضفه في `.env`:
   ```env
   VITE_SENTRY_DSN=your-sentry-dsn-here
   ```

### API Configuration

تأكد من أن `VITE_API_BASE_URL` يشير إلى Backend:

```env
VITE_API_BASE_URL=http://localhost:8001
```

---

## 🐛 حل المشاكل الشائعة

### خطأ في الاتصال بالـ API

- تأكد من تشغيل Backend على المنفذ 8001
- تحقق من `VITE_API_BASE_URL` في `.env`
- أعد تشغيل dev server بعد تغيير `.env`

### خطأ في التبعيات

```bash
# احذف node_modules وأعد التثبيت
rm -rf node_modules package-lock.json
npm install
```

### خطأ في TypeScript

```bash
# فحص الأنواع
npm run type-check
```

---

## 📚 الموارد

- [README.md](./README.md) - دليل شامل
- [USAGE_EXAMPLES.md](./USAGE_EXAMPLES.md) - أمثلة استخدام
- [IMPROVEMENTS.md](./IMPROVEMENTS.md) - التحسينات المنفذة
- [INTEGRATION_COMPLETE.md](./INTEGRATION_COMPLETE.md) - تكامل PWA و Sentry

---

## 🆘 الدعم

إذا واجهت مشاكل:

1. راجع ملفات التوثيق
2. تحقق من `.env` configuration
3. تأكد من تشغيل Backend
4. راجع console للأخطاء

---

**آخر تحديث:** 2024-01-15

