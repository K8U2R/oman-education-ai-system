# قائمة المهام: نظام تعدد اللغات (Tasks)

**الفرع**: `003-i18n-system` | **الحالة**: مخطط (Planned)

## المرحلة 0: البحث والاستكشاف
- [ ] البحث عن دعم `RTL` في مكونات Shadcn/Tailwind الحالية.
- [ ] تحديد مكتبة i18n للـ Backend (Express/Node).

## المرحلة 1: البنية التحتية (Infrastructure)
- [ ] **Frontend**: تثبيت `i18next` و `react-i18next`.
- [ ] **Frontend**: تهيئة `i18n.ts` في `src/presentation/i18n`.
- [ ] **Frontend**: إنشاء ملفات الترجمة الأساسية (`ar/common.json`, `en/common.json`).
- [ ] **Component**: إنشاء مكون `LanguageSwitcher` (زر تبديل).
- [ ] **Layout**: تحديث `html dir` ديناميكياً بناءً على اللغة المختارة.

## المرحلة 2: ترحيل الصفحات (Migration)
### صفحات المصادقة (Auth)
- [ ] استبدال النصوص في `LoginPage.tsx` بمفاتيح ترجمة.
- [ ] استبدال النصوص في `RegisterPage.tsx` بمفاتيح ترجمة.
- [ ] تعريب رسائل التحقق (Validation Messages) في Zod schemas.

### صفحات لوحة التحكم (Dashboard)
- [ ] تعريب نصوص الشريط الجانبي (`Sidebar`).
- [ ] تعريب ترويسة الصفحة (`Header`).
- [ ] تعريب بطاقات الإحصائيات (مثل صفحة Diagnostics التي تم إنشاؤها مؤخراً).

### صفحات القسم الجديد (New Section)
- [ ] التأكد من أن أي تطوير جديد (مثل "نظام اللغة" نفسه كصفحة إعدادات) يبدأ بمفاتيح ترجمة مباشرة.

## المرحلة 3: النظام الخلفي (Backend)
- [ ] تحديث `ErrorHandler` لدعم رسائل مترجمة.
- [ ] توحيد جميع رسائل الخطأ في ملف ثوابت (`constants/messages.ts`).

## المرحلة 4: التحقق
- [ ] اختبار يدوي للغة العربية (تأكد من المحاذاة لليمين).
- [ ] اختبار استمرار اللغة بعد إعادة التحميل.
