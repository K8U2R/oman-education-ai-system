# 🔧 دليل إعداد Gemini API - Gemini API Setup Guide

## ⚠️ المشكلة الحالية

إذا رأيت هذا الخطأ:
```
403 Generative Language API has not been used in project 353597592173 before or it is disabled
```

هذا يعني أن **Gemini API غير مفعّل** في مشروع Google Cloud المرتبط بمفتاح API الخاص بك.

### 🔍 ملاحظات مهمة:

1. **المشروع المرتبط بمفتاح API**: المفتاح `AIzaSyBfRGmWHSoZDp2s-nVjgzj9wIWapSocpzg` مرتبط بمشروع Google Cloud برقم `353597592173`
2. **يجب تفعيل API في نفس المشروع**: يجب تفعيل Generative Language API في المشروع الذي أنشأت فيه المفتاح
3. **انتظار التفعيل**: بعد تفعيل API، قد يستغرق الأمر **5-10 دقائق** حتى يصبح مفعلاً

---

## 📋 خطوات تفعيل Gemini API

### 1️⃣ الوصول إلى Google Cloud Console

1. افتح المتصفح واذهب إلى:
   ```
   https://console.developers.google.com/apis/api/generativelanguage.googleapis.com/overview?project=353597592173
   ```
   
   أو:
   - اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
   - اختر مشروعك (Project ID: `353597592173`)
   - ابحث عن "Generative Language API"

### 2️⃣ تفعيل API

1. في صفحة **Generative Language API**
2. اضغط على زر **"Enable"** (تفعيل)
3. انتظر حتى يتم التفعيل (عادة بضع دقائق)

### 3️⃣ الحصول على API Key

1. اذهب إلى [API Credentials](https://console.cloud.google.com/apis/credentials)
2. اضغط على **"Create Credentials"** → **"API Key"**
3. انسخ المفتاح الذي تم إنشاؤه

### 4️⃣ إضافة API Key إلى المشروع

#### الطريقة 1: ملف `.env`

أضف المفتاح إلى ملف `.env` في جذر المشروع:

```env
GEMINI_API_KEY=your-api-key-here
```

#### الطريقة 2: متغيرات البيئة

في Windows PowerShell:
```powershell
$env:GEMINI_API_KEY="your-api-key-here"
```

في Linux/Mac:
```bash
export GEMINI_API_KEY="your-api-key-here"
```

---

## ✅ التحقق من الإعداد

بعد تفعيل API وإضافة المفتاح:

1. **أعد تشغيل FastAPI server:**
   ```bash
   cd A:\oman-education-ai-system\01-OPERATING-SYSTEM
   python -m api_gateway.fastapi_server
   ```

2. **جرب إرسال رسالة** في واجهة المحادثة

3. **إذا كان كل شيء صحيحاً**، يجب أن تحصل على رد من AI

---

## 🔒 الأمان

⚠️ **مهم جداً:**
- **لا تشارك** مفتاح API الخاص بك
- **لا ترفع** ملف `.env` إلى Git
- استخدم **API Key Restrictions** في Google Cloud Console لتحديد الاستخدام

### تقييد API Key (اختياري لكن موصى به):

1. اذهب إلى [API Credentials](https://console.cloud.google.com/apis/credentials)
2. اختر API Key الخاص بك
3. في **"API restrictions"**:
   - اختر **"Restrict key"**
   - اختر **"Generative Language API"** فقط
4. احفظ التغييرات

---

## 🆘 حل المشاكل

### المشكلة: "403 SERVICE_DISABLED" أو "API غير مفعّل"

**الأسباب المحتملة:**
1. **API غير مفعّل في المشروع**: يجب تفعيل Generative Language API في المشروع المرتبط بالمفتاح
2. **المشروع خاطئ**: المفتاح مرتبط بمشروع مختلف عن الذي تبحث فيه
3. **لم ينتشر التفعيل بعد**: بعد تفعيل API، انتظر 5-10 دقائق

**الحل:**
1. تأكد من أنك في المشروع الصحيح (`353597592173`)
2. اذهب إلى [API Library](https://console.cloud.google.com/apis/library)
3. ابحث عن "Generative Language API"
4. اضغط "Enable"
5. انتظر 5-10 دقائق
6. أعد تشغيل FastAPI server

### المشكلة: "API key not valid"

**الحل:**
- تأكد من نسخ المفتاح بشكل صحيح (بدون مسافات)
- تأكد من أن المفتاح نشط في Google Cloud Console
- تحقق من أن API مفعّل في المشروع
- تأكد من أن المفتاح مرتبط بالمشروع الصحيح

### المشكلة: "Quota exceeded"

**الحل:**
- Gemini API له حد مجاني يومي
- انتظر حتى اليوم التالي أو قم بترقية الحساب

### المشكلة: "Permission denied"

**الحل:**
- تأكد من تفعيل **Generative Language API** في المشروع
- تأكد من أن API Key لديه الصلاحيات اللازمة

---

## 📚 روابط مفيدة

- [Gemini API Documentation](https://ai.google.dev/docs)
- [Google Cloud Console](https://console.cloud.google.com/)
- [API Key Best Practices](https://cloud.google.com/docs/authentication/api-keys)

---

## 💡 ملاحظات

- بعد تفعيل API، قد يستغرق الأمر **بضع دقائق** حتى يصبح مفعلاً
- إذا كنت تستخدم **API Key مجاني**، هناك حدود على عدد الطلبات
- يمكنك مراقبة الاستخدام من [Google Cloud Console](https://console.cloud.google.com/apis/api/generativelanguage.googleapis.com/quotas)

