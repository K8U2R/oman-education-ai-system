# دليل النشر والمراقبة (Sovereign DevOps Guide) 🚀⚙️

هذا الدليل يشرح كيفية المزامنة بين بيئة التطوير المحلية (Local) والخادم الإنتاجي (Production VPS) باستخدام نظام التغذية الراجعة السريعة.

---

## 1. الأوامر المحلية (Local Environment - Windows)

عند الانتهاء من التعديلات محلياً وتريد دفعها للخادم:

```bash
# إضافة التعديلات
git add .

# توثيق التعديل
git commit -m "وصف التعديل هنا"

# الرفع إلى GitHub
git push origin main
```

---

## 2. أوامر الخادم (Production Environment - Remote VPS)

بعد الدفع من جهازك، قم بتشغيل هذه الأوامر على الـ VPS عبر SSH:

### النشر والتحديث (Deploy)

يستخدم هذا الأمر لسحب الكود الجديد، تحديث المكتبات، تشغيل الهجرات (Migrations)، وإعادة بناء الحاويات:

```bash
./deploy.sh
```

### مراقبة السجلات (Monitor)

لمراقبة الأخطاء وحالة النظام في الوقت الفعلي:

```bash
./monitor.sh
```

---

## 3. ملاحظات هامة (Important Notes)

- **صلاحيات التنفيذ:** إذا واجهت مشكلة "Permission Denied"، قم بتشغيل:
  `chmod +x deploy.sh monitor.sh`
- **التوقف عند الخطأ:** سيناريو `deploy.sh` مبرمج ليتوقف فوراً عند حدوث أي خطأ في التحميل أو التثبيت لضمان عدم انهيار النظام.
- **تحديث القاعدة:** يتم تشغيل الهجرات تلقائياً داخل `deploy.sh` عبر محرك `db:migrate` الخاص بالنظام.

---

### 🛡️ مبدأ العمل السيادي
>
> "Monitor locally, fix analytically, deploy atomically."
> راقب محلياً، أصلح تحليلياً، وانشر ذرياً.
