---

description: "قائمة مهام لتنفيذ الميزة"
---

# المهام: [FEATURE NAME]

**المدخلات**: وثائق التصميم من `/specs/[###-feature-name]/`
**المتطلبات الأساسية**: plan.md (مطلوب)، spec.md (مطلوب لقصص المستخدم)، research.md، data-model.md، contracts/

**الخ**تبارات**: الأمثلة أدناه تتضمن مهام اختبار. الاختبارات اختيارية - قم بتضمينها فقط إذا طُلبت صراحة في مواصفات الميزة.

**التنظيم**: يتم تجميع المهام حسب قصة المستخدم لتمكين التنفيذ والاختبار المستقل لكل قصة.

## التنسيق: `[ID] [P?] [Story] الوصف`

- **[P]**: يمكن تشغيله بالتوازي (ملفات مختلفة، لا توجد تبعيات)
- **[Story]**: قصة المستخدم التي تنتمي إليها هذه المهمة (مثلاً US1, US2, US3)
- قم بتضمين مسارات الملفات الدقيقة في الأوصاف

## اتفاقيات المسار (Path Conventions)

- **مشروع مفرد**: `src/`, `tests/` في جذر المستودع
- **تطبيق ويب**: `backend/src/`, `frontend/src/`
- **موبايل**: `api/src/`, `ios/src/` أو `android/src/`
- المسارات الموضحة أدناه تفترض مشروعاً مفرداً - عدلها بناءً على هيكل plan.md

<!-- 
  ============================================================================
  هام: المهام أدناه هي مهام عينة لأغراض التوضيح فقط.
  
  يجب أن يقوم الأمر /speckit.tasks باستبدال هذه المهام بمهام فعلية بناءً على:
  - قصص المستخدم من spec.md (مع أولوياتها P1, P2, P3...)
  - متطلبات الميزة من plan.md
  - الكيانات من data-model.md
  - نقاط النهاية من contracts/
  
  يجب تنظيم المهام حسب قصة المستخدم بحيث يمكن لكل قصة أن تكون:
  - منفذة بشكل مستقل
  - مختبرة بشكل مستقل
  - مسلمة كزيادة في MVP
  
  لا تحتفظ بمهام العينة هذه في ملف tasks.md المولد.
  ============================================================================
-->

## المرحلة 1: الإعداد (البنية التحتية المشتركة)

**الغرض**: تهيئة المشروع والهيكل الأساسي

- [ ] T001 إنشاء هيكل المشروع حسب خطة التنفيذ
- [ ] T002 تهيئة مشروع [اللغة] مع تبعيات [إطار العمل]
- [ ] T003 [P] تكوين أدوات التنسيق والتدقيق (linting)

---

## المرحلة 2: التأسيس (المتطلبات الأساسية المحظورة)

**الغرض**: البنية التحتية الأساسية التي يجب أن تكتمل قبل أن يبدأ تنفيذ أي قصة مستخدم

**⚠️ حرج**: لا يمكن بدء العمل على أي قصة مستخدم حتى تكتمل هذه المرحلة

أمثلة على المهام التأسيسية (عدلها بناءً على مشروعك):

- [ ] T004 إعداد مخطط قاعدة البيانات وإطار الترحيل (migrations)
- [ ] T005 [P] تنفيذ إطار المصادقة/الترخيص
- [ ] T006 [P] إعداد توجيه API وهيكل البرمجيات الوسيطة (middleware)
- [ ] T007 إنشاء النماذج/الكيانات الأساسية التي تعتمد عليها جميع القصص
- [ ] T008 تكوين البنية التحتية لمعالجة الأخطاء والتسجيل
- [ ] T009 إعداد إدارة تكوين البيئة

**نقطة فحص**: الأساس جاهز - يمكن الآن البدء في تنفيذ قصص المستخدم بالتوازي

---

## المرحلة 3: قصة المستخدم 1 - [العنوان] (الأولوية: P1) 🎯 MVP

**الهدف**: [وصف موجز لما تقدمه هذه القصة]

**اختبار مستقل**: [كيفية التحقق من أن هذه القصة تعمل بمفردها]

### اختبارات لقصة المستخدم 1 (اختياري - فقط إذا طلبت الاختبارات) ⚠️

> **ملاحظة: اكتب هذه الاختبارات أولاً، وتأكد من فشلها قبل التنفيذ**

- [ ] T010 [P] [US1] اختبار العقد لـ [نقطة النهاية] في tests/contract/test_[name].py
- [ ] T011 [P] [US1] اختبار التكامل لـ [رحلة المستخدم] في tests/integration/test_[name].py

### تنفيذ قصة المستخدم 1

- [ ] T012 [P] [US1] إنشاء نموذج [Entity1] في src/models/[entity1].py
- [ ] T013 [P] [US1] إنشاء نموذج [Entity2] في src/models/[entity2].py
- [ ] T014 [US1] تنفيذ [Service] في src/services/[service].py (يعتمد على T012, T013)
- [ ] T015 [US1] تنفيذ [نقطة النهاية/الميزة] في src/[الموقع]/[الملف].py
- [ ] T016 [US1] إضافة التحقق ومعالجة الأخطاء
- [ ] T017 [US1] إضافة تسجيل لعمليات قصة المستخدم 1

**نقطة فحص**: في هذه المرحلة، يجب أن تكون قصة المستخدم 1 تعمل بالكامل وقابلة للاختبار بشكل مستقل

---

## المرحلة 4: قصة المستخدم 2 - [العنوان] (الأولوية: P2)

**الهدف**: [وصف موجز لما تقدمه هذه القصة]

**اختبار مستقل**: [كيفية التحقق من أن هذه القصة تعمل بمفردها]

### اختبارات لقصة المستخدم 2 (اختياري - فقط إذا طلبت الاختبارات) ⚠️

- [ ] T018 [P] [US2] اختبار العقد لـ [نقطة النهاية] في tests/contract/test_[name].py
- [ ] T019 [P] [US2] اختبار التكامل لـ [رحلة المستخدم] في tests/integration/test_[name].py

### تنفيذ قصة المستخدم 2

- [ ] T020 [P] [US2] إنشاء نموذج [Entity] في src/models/[entity].py
- [ ] T021 [US2] تنفيذ [Service] في src/services/[service].py
- [ ] T022 [US2] تنفيذ [نقطة النهاية/الميزة] في src/[الموقع]/[الملف].py
- [ ] T023 [US2] التكامل مع مكونات قصة المستخدم 1 (إذا لزم الأمر)

**نقطة فحص**: في هذه المرحلة، يجب أن تعمل قصص المستخدم 1 و 2 بشكل مستقل

---

## المرحلة 5: قصة المستخدم 3 - [العنوان] (الأولوية: P3)

**الهدف**: [وصف موجز لما تقدمه هذه القصة]

**اختبار مستقل**: [كيفية التحقق من أن هذه القصة تعمل بمفردها]

### اختبارات لقصة المستخدم 3 (اختياري - فقط إذا طلبت الاختبارات) ⚠️

- [ ] T024 [P] [US3] اختبار العقد لـ [نقطة النهاية] في tests/contract/test_[name].py
- [ ] T025 [P] [US3] اختبار التكامل لـ [رحلة المستخدم] في tests/integration/test_[name].py

### تنفيذ قصة المستخدم 3

- [ ] T026 [P] [US3] إنشاء نموذج [Entity] في src/models/[entity].py
- [ ] T027 [US3] تنفيذ [Service] في src/services/[service].py
- [ ] T028 [US3] تنفيذ [نقطة النهاية/الميزة] في src/[الموقع]/[الملف].py

**نقطة فحص**: يجب الآن أن تكون جميع قصص المستخدم تعمل بشكل مستقل

---

[أضف المزيد من مراحل قصص المستخدم حسب الحاجة، باتباع نفس النمط]

---

## المرحلة N: التلميع والاهتمامات المشتركة (Polish & Cross-Cutting Concerns)

**الغرض**: التحسينات التي تؤثر على قصص مستخدم متعددة

- [ ] TXXX [P] تحديثات التوثيق في docs/
- [ ] TXXX تنظيف الكود وإعادة الهيكلة Refactoring
- [ ] TXXX تحسين الأداء عبر جميع القصص
- [ ] TXXX [P] اختبارات وحدة إضافية (إذا طُلبت) في tests/unit/
- [ ] TXXX تعزيز الأمان Security Hardening
- [ ] TXXX تشغيل التحقق من quickstart.md

---

## التبعيات وترتيب التنفيذ (Dependencies & Execution Order)

### تبعيات المرحلة

- **الإعداد (المرحلة 1)**: لا توجد تبعيات - يمكن البدء فوراً
- **التأسيس (المرحلة 2)**: يعتمد على اكتمال الإعداد - يحظر (BLOCKS) جميع قصص المستخدم
- **قصص المستخدم (المرحلة 3+)**: تعتمد جميعها على اكتمال المرحلة التأسيسية
  - يمكن لقصص المستخدم المتابعة بالتوازي (إذا توفر الموظجون)
  - أو بالتسلسل حسب ترتيب الأولوية (P1 ← P2 ← P3)
- **التلميع (المرحلة الأخيرة)**: يعتمد على اكتمال جميع قصص المستخدم المطلوبة

### تبعيات قصة المستخدم

- **قصة المستخدم 1 (P1)**: يمكن أن تبدأ بعد التأسيس (المرحلة 2) - لا تبعيات على قصص أخرى
- **قصة المستخدم 2 (P2)**: يمكن أن تبدأ بعد التأسيس (المرحلة 2) - قد تتكامل مع US1 ولكن يجب أن تكون قابلة للاختبار بشكل مستقل
- **قصة المستخدم 3 (P3)**: يمكن أن تبدأ بعد التأسيس (المرحلة 2) - قد تتكامل مع US1/US2 ولكن يجب أن تكون قابلة للاختبار بشكل مستقل

### داخل كل قصة مستخدم

- الاختبارات (إذا ضمنت) يجب كتابتها وتفشل قبل التنفيذ
- النماذج قبل الخدمات
- الخدمات قبل نقاط النهاية
- التنفيذ الأساسي قبل التكامل
- اكتمال القصة قبل الانتقال للأولوية التالية

### الفرص المتوازية

- جميع مهام الإعداد الموسومة بـ [P] يمكن تشغيلها بالتوازي
- جميع المهام التأسيسية الموسومة بـ [P] يمكن تشغيلها بالتوازي (داخل المرحلة 2)
- بمجرد اكتمال المرحلة التأسيسية، يمكن أن تبدأ جميع قصص المستخدم بالتوازي (إذا سمحت سعة الفريق)
- جميع الاختبارات لقصة مستخدم الموسومة بـ [P] يمكن تشغيلها بالتوازي
- النماذج داخل قصة الموسومة بـ [P] يمكن تشغيلها بالتوازي
- قصص مستخدم مختلفة يمكن العمل عليها بالتوازي من قبل أعضاء فريق مختلفين

---

## مثال على التوازي: قصة المستخدم 1

```bash
# تشغيل جميع الاختبارات لقصة المستخدم 1 معاً (إذا طُلبت الاختبارات):
Task: "Contract test for [endpoint] in tests/contract/test_[name].py"
Task: "Integration test for [user journey] in tests/integration/test_[name].py"

# تشغيل جميع النماذج لقصة المستخدم 1 معاً:
Task: "Create [Entity1] model in src/models/[entity1].py"
Task: "Create [Entity2] model in src/models/[entity2].py"
```

---

## استراتيجية التنفيذ (Implementation Strategy)

### MVP أولاً (قصة المستخدم 1 فقط)

1. إكمال المرحلة 1: الإعداد
2. إكمال المرحلة 2: التأسيس (حرج - يحظر جميع القصص)
3. إكمال المرحلة 3: قصة المستخدم 1
4. **توقف وتحقق**: اختبر قصة المستخدم 1 بشكل مستقل
5. النشر/العرض التجريبي إذا كان جاهزاً

### التسليم التزايدي (Incremental Delivery)

1. إكمال الإعداد + التأسيس ← الأساس جاهز
2. إضافة قصة المستخدم 1 ← اختبار مستقل ← نشر/عرض (MVP!)
3. إضافة قصة المستخدم 2 ← اختبار مستقل ← نشر/عرض
4. إضافة قصة المستخدم 3 ← اختبار مستقل ← نشر/عرض
5. كل قصة تضيف قيمة دون كسر القصص السابقة

### استراتيجية الفريق المتوازي

مع وجود مطورين متعددين:

1. يكمل الفريق الإعداد + التأسيس معاً
2. بمجرد انتهاء التأسيس:
   - المطور أ: قصة المستخدم 1
   - المطور ب: قصة المستخدم 2
   - المطور ج: قصة المستخدم 3
3. تكتمل القصص وتتكامل بشكل مستقل

---

## ملاحظات

- مهام [P] = ملفات مختلفة، لا توجد تبعيات
- تسمية [Story] تعين المهمة لقصة مستخدم محددة للتتبع
- كل قصة مستخدم يجب أن تكون قابلة للاكمال والاختبار بشكل مستقل
- تحقق من فشل الاختبارات قبل التنفيذ
- قم بالالتزام (Commit) بعد كل مهمة أو مجموعة منطقية
- توقف عند أي نقطة فحص للتحقق من القصة بشكل مستقل
- تجنب: المهام الغامضة، تعارضات الملف نفسه، التبعيات المتبادلة بين القصص التي تكسر الاستقلال
